# 🚀 NEOVIM PYTHON IDE - ФИНАЛЬНАЯ ИНСТРУКЦИЯ

## 📊 АНАЛИЗ КОНФИГУРАЦИИ

Вот что было исправлено и оптимизировано в репозитории:

### ✅ ИСПРАВЛЕННЫЕ ПРОБЛЕМЫ

1. **mason.lua** - Удалена блокирующая строка `if true then return {} end`
   - Теперь Mason загружается правильно
   - pyright добавлен в `ensure_installed` (строка 11)
   - Гарантирует автоматическую установку pyright при запуске

2. **astrolsp.lua** - Pyright включен и активирован
   - Раскомментирована строка `"pyright",` в конфиге серверов
   - Файл разблокирован (удалена `if true then return {} end`)

3. **python.lua** - Чистая конфигурация DAP без конфликтов
   - nvim-dap для отладки
   - nvim-dap-python для Python-специфичной отладки
   - nvim-dap-ui для интерфейса отладки

4. **ide.lua** - Полный IDE функционал
   - trouble.nvim (диагностика)
   - symbols-outline.nvim (структура файла)
   - nvim-navic (навигация)
   - todo-comments.nvim (TODO/FIXME)
   - gitsigns.nvim (git blame)

5. **clipboard.lua** - Поддержка SSH буфера обмена
   - nvim-osc52 для OSC 52
   - Копирование работает даже через SSH

---

## 🎯 БЫСТРЫЙ СТАРТ (3 ШАГА)

### Шаг 1: Подготовка окружения
```bash
# Убедись что Python3 установлен
python3 --version  # Должна быть 3.8+

# Установи pynvim (КРИТИЧНО для Neovim)
pip install --user pynvim

# Проверь что всё OK
python3 -m pip list | grep pynvim
```

### Шаг 2: Запусти Neovim
```bash
# Просто открой Neovim - Mason установит всё автоматически
nvim

# Mason установит (при первом запуске):
# ✓ pyright (LSP для Python)
# ✓ black (форматер)
# ✓ ruff (линтер)
# ✓ debugpy (отладчик)
# ✓ lua-language-server
# ✓ stylua
# ✓ tree-sitter-cli
```

### Шаг 3: Проверь что всё работает
```vim
# В Neovim выполни
:checkhealth python3      # Должно быть "OK"
:LspInfo                   # Должен быть pyright
:Mason                     # Проверить установку инструментов
```

---

## 📝 ФАЙЛЫ КОНФИГУРАЦИИ (АНАЛИЗ)

### lua/plugins/mason.lua (19 строк)
✅ **Назначение**: Установка инструментов
- Загружает williamboman/mason.nvim
- ensure_installed содержит все необходимые инструменты
- pyright на строке 11 - КРИТИЧНО
- black, ruff, debugpy - для Python

### lua/plugins/astrolsp.lua
✅ **Назначение**: Конфигурация LSP
- pyright включен в список серверов
- Обработка Python через Pyright
- Форматирование и диагностика

### lua/plugins/python.lua (54 строки)
✅ **Назначение**: DAP (отладка)
- nvim-dap (отладчик)
- nvim-dap-python (интеграция с Python)
- nvim-dap-ui (UI для отладки)
- Горячие клавиши F5-F12

### lua/plugins/ide.lua (84 строки)
✅ **Назначение**: IDE функции
- trouble.nvim - панель диагностики
- symbols-outline.nvim - структура файла
- nvim-navic - breadcrumb навигация
- todo-comments.nvim - подсветка комментариев
- gitsigns.nvim - git информация

### lua/plugins/clipboard.lua (27 строк)
✅ **Назначение**: Clipboard для SSH
- nvim-osc52 - поддержка OSC 52
- Копирование через SSH (даже без X11)
- <Space>c для копирования

### lua/plugins/none-ls.lua
✅ **Назначение**: Форматирование и линтинг
- Форматирование при сохранении
- Интеграция с black и ruff

### lua/plugins/astrocore.lua
✅ **Назначение**: Ядро конфигурации
- Основные настройки Neovim
- Горячие клавиши
- leader key = пробел (Space)

---

## ⌨️ ПОЛНАЯ ТАБЛИЦА ГОРЯЧИХ КЛАВИШ

### LSP & Intellisense
| Клавиша | Действие |
|---------|----------|
| K | Документация (hover) |
| gd | Goto definition |
| gr | Find references |
| gI | Goto implementation |
| <Space>rn | Rename symbol |
| <Space>ca | Code actions |
| <Space>lf | Format |

### Отладка (DAP)
| Клавиша | Действие |
|---------|----------|
| <F5> | Запуск/продолжить |
| <F10> | Step over |
| <F11> | Step into |
| <F12> | Step out |
| <Space>b | Toggle breakpoint |
| <Space>B | Conditional breakpoint |

### IDE
| Команда | Действие |
|---------|----------|
| :Trouble | Диагностика |
| :SymbolsOutline | Структура файла |
| :TodoTelescope | TODO/FIXME |

### Clipboard
| Клавиша | Действие |
|---------|----------|
| <Space>c | Copy (visual) |
| <Space>cc | Copy line |

---

## 🛠️ ПОЛНЫЙ WORKFLOW

### Создание проекта
```bash
# 1. Создай папку
mkdir my_python_project
cd my_python_project

# 2. Создай virtual environment
python -m venv .venv
source .venv/bin/activate  # Unix/macOS
# или
.venv\\Scripts\\activate  # Windows

# 3. Установи debugpy в проект
pip install debugpy

# 4. Создай main.py
cat > main.py << 'EOF'
def fibonacci(n: int) -> int:
    """Calculate Fibonacci number."""
    if n <= 1:
        return n
    return fibonacci(n-1) + fibonacci(n-2)

if __name__ == "__main__":
    result = fibonacci(10)
    print(f"Fibonacci(10) = {result}")
EOF
```

### Использование в Neovim
```bash
# 5. Открой проект в Neovim
nvim main.py

# 6. В Neovim (все клавиши начинаются с пробела = Space)
# Наведись на функцию fibonacci и нажми K -> увидишь docstring
# Нажми gd -> перейдет к определению
# Нажми Space+b на строке со sleep -> установит breakpoint
# Нажми F5 -> запустится отладка
# Нажми F10 -> шаг over
# Нажми F11 -> шаг into
# Нажми Space+lf -> отформатирует код (black)

# 7. Для копирования через SSH
# Выбери текст (v mode)
# Нажми Space+c -> скопируется в буфер обмена
```

---

## ✅ КОНТРОЛЬНЫЙ СПИСОК

После установки проверь:

- [ ] `pip install --user pynvim` выполнен
- [ ] `pip install --user debugpy` выполнен
- [ ] Neovim запущен и Mason установил инструменты
- [ ] `:checkhealth python3` показывает "OK"
- [ ] `:LspInfo` показывает pyright attached
- [ ] Создан Python файл с функцией
- [ ] Hover (K) показывает документацию
- [ ] gd показывает перейти к определению
- [ ] Breakpoint (<Space>b) устанавливается
- [ ] F5 запускает отладку с UI
- [ ] Форматирование (<Space>lf) работает

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Pyright ошибка: "not installed, missing from PATH"

**РЕШЕНИЕ 1**: Переустанови через Mason
```vim
:Mason
# Найди pyright в списке и нажми 'i' для установки
```

**РЕШЕНИЕ 2**: Установи глобально
```bash
pip install pyright
```

**РЕШЕНИЕ 3**: Проверь PATH
```bash
which pyright
pyright --version
```

### LSP не подключается
```vim
# Проверь
:LspInfo
:checkhealth lsp

# Перезагрузи Neovim
:q
nvim
```

### Отладка не работает
```bash
# 1. Убедись что debugpy установлен
pip install debugpy

# 2. В Neovim
:checkhealth dap

# 3. Установи breakpoint в коде
# <Space>b

# 4. Запусти отладку
# <F5>
```

### SSH clipboard не работает
```bash
# Подключись с поддержкой TERM_PROGRAM
ssh -o SetEnv=TERM_PROGRAM=tmux user@host

# В SSH терминале
echo $TERM_PROGRAM  # Должно быть "tmux"

# Копируй в Neovim
# <Space>c (visual mode)
```

---

## 📚 РЕСУРСЫ

- [Pyright Documentation](https://github.com/microsoft/pylance-release)
- [nvim-dap Wiki](https://github.com/mfussenegger/nvim-dap/wiki)
- [AstroNvim Docs](https://docs.astronvim.com/)
- [Black Formatter](https://black.readthedocs.io/)
- [Ruff Linter](https://github.com/astral-sh/ruff)

---

## ✨ SUMMARY

Теперь у тебя:
✅ Полностью функциональная Python IDE в Neovim
✅ Автоматическая установка всех инструментов
✅ Работающий LSP (Pyright) с hover и goto
✅ Интерактивная отладка с breakpoints
✅ Форматирование и лinting (black + ruff)
✅ Git интеграция с inline blame
✅ SSH поддержка для буфера обмена
✅ IDE функции (структура, диагностика, todos)

Всё работает как одна согласованная система! 🎉
