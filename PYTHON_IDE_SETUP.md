# 🐍 Полная инструкция PyCharm-like IDE в Neovim

## 📋 Быстрый старт (5 минут)

### Шаг 1: Обновить конфиг
```bash
cd ~/.config/nvim
git pull origin main
```

### Шаг 2: Установить инструменты
```bash
# Метод 1: Через Mason (автоматический)
nvim
:Mason
# Найти и установить: basedpyright, black, ruff, debugpy

# Метод 2: Через pip (если Mason не работает)
pip install --user basedpyright black ruff debugpy
```

### Шаг 3: Перезагрузить Neovim
```bash
# Закрыть и открыть заново
:quit
nvim
```

### Шаг 4: Проверить что работает
```bash
# В Neovim
:checkhealth python3
:LspInfo
# Должен показать: basedpyright running
```

## 🎮 Горячие клавиши (PyCharm функционал)

### Основная навигация
| Действие | Клавиша | Результат |
|----------|---------|----------|
| Hover (информация) | `K` | Показывает тип, docstring |
| Go to definition | `gd` | Прыгает к определению класса/функции |
| Go to references | `gr` | Все места где используется |
| Find all references | `:Telescope lsp_references` | Список всех ссылок |
| Rename | `:LspRename` | Переименовать везде |
| Diagnostics | `:Telescope diagnostics` | Все ошибки в проекте |

### Автодополнение (IntelliSense)
| Действие | Клавиша | |
|----------|---------|---|
| Включить автодополнение | `Ctrl+x Ctrl+o` | Показывает методы/поля |
| Выбрать из списка | `Ctrl+n` / `Ctrl+p` | Навигация вверх/вниз |
| Подтвердить | `Enter` | Вставить выбранный вариант |
| Отменить | `Esc` | Закрыть список |

### Отладка (Debugger)
| Действие | Клавиша | |
|----------|---------|---|
| Поставить breakpoint | `<Space>b` | Красная точка на строке |
| Запустить отладку | `<F5>` | Выполнить до breakpoint |
| Step Over | `<F10>` | Следующая строка |
| Step Into | `<F11>` | Войти в функцию |
| Continue | `<F5>` (в отладке) | Продолжить выполнение |

### Рефакторинг
| Действие | Команда |
|----------|----------|
| Форматирование | `:LspFormat` |
| Исправить ошибку | `:CodeAction` |
| Найти проблемы | `:Telescope diagnostics` |

## 🐛 Тестирование конфигурации

### Создать тестовый файл
```bash
cat > test.py << 'EOF'
class Calculator:
    """Простой калькулятор"""
    
    def add(self, a: int, b: int) -> int:
        """Сложить два числа"""
        return a + b
    
    def multiply(self, x: float, y: float) -> float:
        """Умножить два числа"""
        return x * y

if __name__ == "__main__":
    calc = Calculator()
    result = calc.add(5, 3)
    print(f"Result: {result}")
EOF

# Открыть в Neovim
nvim test.py
```

### Проверить функционал

1. **Hover (K)**:
   - Наведи на `Calculator` → должна показать docstring класса
   - Наведи на `add` → должна показать подпись функции

2. **Автодополнение (Ctrl+x Ctrl+o)**:
   - На новой строке: `calc.`
   - Нажми `Ctrl+x Ctrl+o` → должны показать `add`, `multiply`

3. **Go to definition (gd)**:
   - Наведи курсор на `Calculator` в главной части
   - Нажми `gd` → должен прыгнуть на определение класса

4. **Find references (gr)**:
   - Наведи на `add` в определении
   - Нажми `gr` → должен показать где используется

## ⚙️ Конфигурация

### Основные файлы

**lua/plugins/mason.lua** - установка инструментов
```lua
ensure_installed = {
  "basedpyright",  -- Python LSP (как PyCharm)
  "black",         -- Форматер (как PyCharm formatter)
  "ruff",          -- Линтер (как PyCharm code inspection)
  "debugpy",       -- Отладчик (как PyCharm debugger)
}
```

**lua/plugins/astrolsp.lua** - конфигурация LSP
- Настраивает basedpyright
- Включает hover, completion, go-to-definition

**lua/plugins/python.lua** - DAP (Debug Adapter Protocol)
- Конфигурация отладчика debugpy
- Автоматическое открытие UI при отладке

**lua/plugins/ide.lua** - IDE функции
- Trouble (список ошибок, как Problems panel)
- Symbols (иерархия кода)
- Todo comments (найти TODO)

**lua/plugins/clipboard.lua** - буфер обмена для SSH

## 🔧 Решение проблем

### ❌ "basedpyright-langserver not found"

**Решение 1: Проверить установку через Mason**
```bash
nvim
:Mason
# basedpyright должен быть зелёным (installed)
```

**Решение 2: Установить вручную**
```bash
# Проверить что pip работает
pip --version

# Установить basedpyright
pip install --user basedpyright

# Проверить что установлено
basedpyright --version
```

**Решение 3: Проверить PATH**
```bash
# Проверить что basedpyright-langserver в PATH
which basedpyright-langserver

# Или полный путь
python -m basedpyright --langserver
```

### ❌ "LSP не подключается"

```bash
# Полностью перезагрузить Neovim
:quit
nvim

# Или вручную перезагрузить LSP
:LspRestart

# Проверить статус
:LspInfo
```

### ❌ "Нет автодополнения"

```bash
# Проверить что комплишн плагин установлен
# В Neovim:
:Mason
# Должны быть установлены инструменты

# Попробовать команду вручную
:LspInfo
# Должен показать capabilities включая completion
```

### ❌ "Hover не показывает информацию"

```bash
# Проверить что файл Python
:set filetype
# Должно быть: filetype=python

# Проверить что LSP активен
:LspInfo

# Попробовать на встроенной функции
# Наведи на print или len, нажми K
```

### ❌ "Отладка не работает"

```bash
# Проверить debugpy
pip install debugpy

# В Neovim проверить
:checkhealth dap

# Проверить keymaps
:LspInfo
# Должны быть DAP keybindings
```

## 🎯 PyCharm vs Neovim сравнение

| Функция | PyCharm | Neovim |
|---------|---------|--------|
| Hover info | Ctrl+Q | `K` |
| Go to definition | Ctrl+B | `gd` |
| Find references | Ctrl+Alt+F7 | `gr` |
| Rename | Shift+F6 | `:LspRename` |
| Reformat code | Ctrl+Alt+L | `:LspFormat` |
| Run/Debug | Shift+F10 | `<F5>` |
| Breakpoint | Ctrl+F8 | `<Space>b` |
| Step Over | F8 | `<F10>` |
| Step Into | F7 | `<F11>` |
| Completion | Ctrl+Space | `Ctrl+x Ctrl+o` |
| Find in project | Ctrl+Shift+F | `:Telescope live_grep` |
| Find file | Ctrl+Shift+N | `:Telescope find_files` |

## 📚 Дополнительные ресурсы

- **BasedPyright docs**: https://detachhead.github.io/basedpyright/
- **Neovim LSP docs**: https://neovim.io/doc/user/lsp.html
- **AstroNvim docs**: https://docs.astronvim.com/

## 🆘 Когда ничего не помогает

```bash
# 1. Очистить кэш Mason
rm -rf ~/.local/share/nvim/mason

# 2. Переустановить через Mason
nvim
:Mason
# Установить заново basedpyright, black, ruff, debugpy

# 3. Проверить логи LSP
:LspLog

# 4. Полностью очистить и переинсталлировать
cd ~/.config/nvim
git pull origin main
rm -rf ~/.local/share/nvim/mason
nvim
:Mason  # Установить все
:LspRestart
```

## ✅ Что вы получите

✨ **Полный функционал PyCharm IDE:**
- Intelligent code completion (методы, поля, параметры)
- Hover documentation (docstrings, типы)
- Go to definition/references (быстрая навигация)
- Error highlighting и inspection (как Problems panel)
- Code formatting (Black)
- Linting (Ruff)
- Debugging with breakpoints (debugpy)
- Git integration (inline blame)
- SSH support (OSC52 clipboard)

🚀 **Работает везде:**
- На локальной машине
- По SSH на удалённом сервере
- В Docker контейнере
- На Linux, macOS, Windows

💪 **Преимущества перед PyCharm:**
- Молниеносно быстро (стартует < 100ms)
- Расходует минимум памяти
- Полная свобода в кастомизации
- Работает в терминале везде
- Модульная архитектура (отключи что не нужно)

---

**Версия**: 2025-12-25
**AstroNvim**: v5+
**Neovim**: 0.7+
**Python**: 3.8+
